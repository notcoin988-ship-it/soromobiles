/**
 * Список языков (§9) — отдельным файлом БЕЗ импортов.
 *
 * Нужен модулям, которым нельзя тянуть i18next и react-i18next: разбор
 * сохранённых настроек проверяется тестами в node, а react-i18next там не
 * поднять. Дублировать список нельзя — разъедется.
 */

export const LANGUAGES = ['tg', 'ru', 'en'] as const;
export type Language = (typeof LANGUAGES)[number];

/**
 * Таджикский, НЕЗАВИСИМО от языка системы. Это требование §9, а не упущение:
 * аудитория — таджикоязычные школы, а телефон у учителя чаще русский.
 */
export const DEFAULT_LANGUAGE: Language = 'tg';
