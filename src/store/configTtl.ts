/**
 * Срок жизни конфигурации (§6.6): «запрашивается при каждом холодном старте,
 * кэшируется на 24 часа».
 *
 * Отдельным файлом без импортов, потому что значение нужно и сторам, и
 * клиенту TanStack Query, а тот не должен тянуть за собой react-native через
 * цепочку store/config.ts.
 */
export const CONFIG_TTL_MS = 24 * 60 * 60 * 1000;
